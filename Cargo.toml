[package]
name = "hylic"
version = "0.1.0"
edition = "2024"
description = "Composable tree computation framework: monoidal catamorphisms with parallel execution"

[dependencies]
either = "1.15.0"
derive_more = { version = "2.0.1", features = ["full"] }
atomic-wait = "1"
