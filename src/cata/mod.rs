//! cata — execution machinery (currently `exec`).
//!
//! The pipeline typestate layer has been extracted to the
//! sibling `hylic-pipeline` crate. `hylic::cata::exec` stays
//! here as the core executor module.

pub mod exec;
