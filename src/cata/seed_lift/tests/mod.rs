//! Tests. Phase-3 note: pre-CPS test files (core, transforms, entry)
//! are disabled during the refactor; they'll be replaced by the
//! `power_user/` suite once the CPS API is complete.

// Pre-CPS tests — targeting old Lift<N, N2> API and old pipeline
// sugar methods. Disabled until superseded.
//
// mod core;
// mod transforms;
// mod entry;

pub mod explorative;
